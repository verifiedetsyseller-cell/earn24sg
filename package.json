{
  "name": "earn24sg-backend",
  "version": "1.0.0",
  "description": "Backend server for the EARN24 SG platform.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.0",
    "nanoid": "^3.3.7"
  }
}
```

### How to Use This File:

1.  **Save:** Save this code as `package.json` in the same local folder where you have your `server.js` file.
2.  **Install Dependencies:** Open your terminal, navigate to that folder, and run the command:
    ```bash
    npm install
    

